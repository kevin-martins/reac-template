import Wrapper from '../../../components/Wrapper';
import HeroImageScroll from '../../../components/heros/HeroImageScroll';

const HeroPage = () => {
  return (
    <div>
      <Wrapper title='Hero Image Scroll' padding={false}>
        <HeroImageScroll />
      </Wrapper>
    </div>
  );
};

export default HeroPage;
